import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
  cardIcon: { height: '2rem' },
  grid: { padding: 16, width: theme.spacing(32) },
}));

export default useStyles;
