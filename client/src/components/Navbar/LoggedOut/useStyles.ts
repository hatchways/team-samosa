import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
  button: { marginRight: '0.5rem', paddingLeft: theme.spacing(4), paddingRight: theme.spacing(4) },
}));

export default useStyles;
