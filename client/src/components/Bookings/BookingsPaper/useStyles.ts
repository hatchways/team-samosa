import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    padding: 32,
    minHeight: '100px',
  },
  empty: { fontSize: 16 },
}));

export default useStyles;
